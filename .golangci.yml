version: "2"

linters:
  default: standard
  enable:
    - revive
    - misspell
    - unconvert
    - gocritic
    - bodyclose
    - nilerr

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/0xnikshi/keva
